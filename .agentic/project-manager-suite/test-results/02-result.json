{
  "scenario": "02",
  "title": "Story 02: Bug Fix Request — Full Lifecycle",
  "team": "android",
  "loop": "work",
  "pmResponse": "# Action Plan: Bug Fix — \"App crashes on orientation change during checkout screen\"\n\n## Analysis\n\n**Issue type:** Bug (not a feature — something is broken).\n**Component:** Frontend / Android — the crash occurs on a checkout *screen* (UI lifecycle on orientation change is a classic Android config-change problem).\n**Phase flow for a Frontend Bug:** Reproduce → Dev → Validate (no separate release phase — Android redeploys through Play Store review, which is a separate flow not requested here).\n**Bug rule:** Bugs start with **reproduction by the validator**, not planning. So Phase 1 goes to `android-validator`, not `android-developer`.\n**Child count:** 3 phase tasks + 1 sync tracker = **4 children**.\n\n---\n\n## Final issue hierarchy\n\n```\nBUG (P)  \"Fix: App crashes on orientation change during checkout screen\"\n  tag: bug | assignee: project-manager | status: in-progress\n│\n├── Task \"Reproduce checkout-screen crash on orientation change\"   ← Phase 1\n│     tag: task | assignee: android-validator | status: todo\n│\n├── Task \"Fix checkout-screen orientation-change crash\"            ← Phase 2  (blocked by Phase 1)\n│     tag: task | assignee: android-developer | status: todo\n│\n├── Task \"Validate fix with regression tests across API levels\"    ← Phase 3  (blocked by Phase 2)\n│     tag: task | assignee: android-validator | status: todo\n│\n└── Task \"Verify bug fix complete\"                                 ← Sync tracker (blocked by Phase 3)\n      tag: task,sync | assignee: project-manager | status: todo\n```\n\n---\n\n## Commands, in execution order\n\n### Step 1 — Create the parent bug (status `todo` initially)\n\n```\nagt create \"Fix: App crashes on orientation change during checkout screen\" \\\n  --tags bug \\\n  --assignee project-manager \\\n  --status todo \\\n  --priority 3 \\\n  --description \"User-reported crash: the app crashes when the device orientation changes while the checkout screen is visible. Frontend bug — reproduction-first flow (Reproduce → Dev → Validate). No separate release phase for this bug.\"\n```\n→ Assume this returns **`<P>`**.\n\n**Reasoning:** The deliverable is a bug, so the structural tag is `bug` (exactly one hierarchy tag). The PM owns the parent. I start it at `todo` and flip it to `in-progress` only after all children exist. Priority 3 because a crash on a checkout (revenue) screen is important but not a confirmed production hotfix.\n\n---\n\n### Step 2 — Create Phase 1: Reproduce (validator)\n\n```\nagt create \"Reproduce checkout-screen crash on orientation change\" \\\n  --tags task \\\n  --assignee android-validator \\\n  --status todo \\\n  --parentId <P>\n```\n→ Assume this returns **`<T1>`**.\n\n**Reasoning:** Bugs start with reproduction by the **validator**, never planning and never the developer. `android-validator` is the correct owner — they can reproduce UI/lifecycle bugs and produce a technical diagnosis. One task per phase; structural tag is exactly `task`.\n\n---\n\n### Step 3 — Create Phase 2: Dev (developer)\n\n```\nagt create \"Fix checkout-screen orientation-change crash\" \\\n  --tags task \\\n  --assignee android-developer \\\n  --status todo \\\n  --parentId <P>\n```\n→ Assume this returns **`T2>**.\n\n**Reasoning:** Development goes to `android-developer` (never a validator). This task will be blocked by the reproduction task so the developer works from a real diagnosis rather than guesswork.\n\n---\n\n### Step 4 — Create Phase 3: Validate (validator)\n\n```\nagt create \"Validate fix with regression tests across API levels\" \\\n  --tags task \\\n  --assignee android-validator \\\n  --status todo \\\n  --parentId <P>\n```\n→ Assume this returns **`<T3>`**.\n\n**Reasoning:** Validation is ALWAYS a separate task assigned to the validator, and never the same agent who implemented the fix. Even though `android-validator` also did reproduction, that's fine — reproduction and final validation are distinct phases. Frontend bugs have no separate Release phase, so this is the last worker phase.\n\n---\n\n### Step 5 — Create the sync tracker (the \"+1\" child)\n\n```\nagt create \"Verify bug fix complete\" \\\n  --tags task,sync \\\n  --assignee project-manager \\\n  --status todo \\\n  --parentId <P>\n```\n→ Assume this returns **`<S>`**.\n\n**Reasoning:** Rule 1 — every parent gets a sync tracker child in addition to its phase tasks. Tag is exactly `task,sync` (both words, never just `sync`). Assigned to me, status `todo` (never `in-progress`). I do not activate or monitor it; the system hands it to me when its blockage clears.\n\n---\n\n### Step 6 — Create the phase chain blockages\n\n```\nagt blockages add <T2> --by <T1>\nagt blockages add <T3> --by <T2>\nagt blockages add <S>  --by <T3>\n```\n\n**Reasoning:** Sequential chain so work flows in order:\n- Dev (T2) waits for the validator's reproduction/diagnosis.\n- Final Validate (T3) waits for the fix.\n- Sync tracker (S) is blocked by the **last worker** (T3). While T3 is working, the sync tracker stays blocked and the loop skips me. When T3 marks itself `done`, the system **auto-clears** S's blockage → the loop hands S to me → I verify and close the parent. (I never run `agt blockages resolve` — the system does it.)\n\n---\n\n### Step 7 — Add guiding comments to each task\n\n```\nagt comments add <P> --content \"Frontend bug. Flow: Reproduce (android-validator) → Dev (android-developer) → Validate (android-validator). No separate release phase. PM owns parent + sync tracker statuses only; workers drive their own task statuses from step 3 onward.\"\n\nagt comments add <T1> --content \"Reproduce the crash on orientation change during checkout. Capture: device model, Android API level, full stack trace / logcat, and the Activity/Fragment involved. Provide a root-cause hypothesis (e.g., missing configChanges handling, lost instance state on recreate, unretained listener on the Activity, or a dialog/FragmentManager transaction after state save). This diagnosis guides the developer's fix.\"\n\nagt comments add <T2> --content \"Implement the fix based on android-validator's reproduction report (see comments on the Reproduce task). Likely involves one or more of: handling configChanges / properly saving & restoring instance state, moving state into a ViewModel, or guarding Fragment transactions against saved-state. Report what was changed and why in a comment when done.\"\n\nagt comments add <T3> --content \"Verify the fix: confirm no crash on orientation change during checkout across multiple API levels and screen sizes. Add a regression test (e.g., UI test rotating the Activity during checkout). If the fix does not hold or new issues appear, mark this task todo and reassign to project-manager with findings — do NOT fix it yourself.\"\n\nagt comments add <S>  --content \"Sync tracker. Blocked by the Validate task. When unblocked, verify the Validate task is done and comments show a passing regression, then mark this done and close the parent bug.\"\n```\n\n**Reasoning:** Comments give each worker a crisp brief so the loop can run without me. The validator's comment tells them to **diagnose, not fix** (rule 5). The validate comment explicitly tells them to route failures back to me rather than fixing — a validator never fixes.\n\n---\n\n### Step 8 — Flip the parent to `in-progress` LAST\n\n```\nagt update <P> --status in-progress\n```\n\n**Reasoning:** Rule 2 — set the parent to `in-progress` only after all children exist. This is the final creation step. From here the work loop takes over: T1 has no open blockages → the loop hands it to `android-validator`.\n\n---\n\n## What happens next (I do nothing)\n\n1. **Reproduce (T1)** unblocked → loop hands it to `android-validator` → they set `in-progress`, reproduce + diagnose, comment with findings, mark `done`.\n2. T1 `done` → **system auto-clears** T2's blockage → loop hands T2 to `android-developer` → they implement, comment, mark `done`.\n3. T2 `done` → auto-clears T3 → `android-validator` runs regression tests, marks `done`.\n4. T3 `done` → auto-clears the **sync tracker (S)** → loop hands S to me.\n5. I read T3's comments, confirm the fix validates, mark **S `done`**, then mark **`<P>` `done`**.\n\n---\n\n## What I deliberately do NOT do\n\n- **No `agt blockages resolve`** anywhere — the system auto-resolves blockages when the blocking issue is marked `done`.\n- **No status changes to T1/T2/T3** — workers drive `todo → in-progress → done` on their own children.\n- **No planning task** — bugs start with reproduction, not planning.\n- **No release phase** — frontend bugs follow Reproduce → Dev → Validate; Play Store resubmission is a separate non-standard flow not requested here.\n- **No second component** — the request names one bug on one screen; I don't add a backend track or any scope creep.\n\n**Total commands: 5 creates + 3 blockages + 5 comments + 1 status update = 14 `agt` commands**, executed in the order shown.",
  "scores": {
    "hierarchy": 10,
    "assignments": 10,
    "dependencies": 10,
    "syncPattern": 10,
    "statusManagement": 10,
    "behavioralAccuracy": 10,
    "completeness": 10
  },
  "totalScore": 70,
  "maxScore": 70,
  "pass": true,
  "feedback": "Excellent, near-perfect response. The agent correctly built the full hierarchy: a bug parent owned by project-manager with 3 sequential phase tasks + 1 sync tracker child. Assignments are spot-on — reproduction/validation to android-validator, development to android-developer, sync to project-manager, faithfully reflecting the bugs-start-with-reproduction-by-validator rule. The dependency chain (T2←T1, T3←T2, S←T3) exactly matches expected, with the sync tracker blocked by the last worker. Status management is correct: parent starts todo and flips to in-progress only after all children exist, children stay todo, and the PM never touches child statuses. Key behaviors are all met: no planning task, no release phase (frontend bug), validator diagnosed-not-fixes, auto-resolving blockages, and explicit device/API-level specifics in comments. Only trivial nit: phase labels (reproduction/development/validation) are conveyed narratively rather than as an explicit --phase flag, but this doesn't affect correctness."
}